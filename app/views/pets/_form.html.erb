<%= simple_form_for @pet do |f| %>
  <%= f.input :name %>
  <%= f.input :species, collection: %w(dog cat rabbit) %>
  <%= f.input :address %>
  <%= f.input :date_found %>
  <%= f.button :submit %>
<% end %>
